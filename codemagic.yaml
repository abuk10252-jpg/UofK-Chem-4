workflows:
  react-native-android:
    name: React Native Android Release Build
    max_build_duration: 120
    instance_type: mac_mini_m2
    
    environment:
      android_signing:
        -my-app-production-keystore
      vars:
        PACKAGE_NAME: "com.abuk10252.uofkchem"
        EXPO_PUBLIC_API_URL: "https://uofk-chem-server.onrender.com/api"
        CI: "1"
    
    scripts:
      - name: Install dependencies
        script: |
          npm install --legacy-peer-deps
      
      - name: Create .env file
        script: |
          cat > .env << EOF
          EXPO_PUBLIC_API_URL=https://uofk-chem-server.onrender.com/api
          EOF
      
      - name: Expo Prebuild
        script: |
          npx expo prebuild --platform android --clean
      
      - name: Configure Android Project
        script: |
          echo "sdk.dir=$ANDROID_SDK_ROOT" > android/local.properties
          cd android
          chmod +x gradlew
      
      - name: Build Android RELEASE APK
        script: |
          cd android
          ./gradlew clean
          ./gradlew assembleRelease -PversionCode=$BUILD_NUMBER -PversionName=1.0.$BUILD_NUMBER
    
    artifacts:
      - android/app/build/outputs/apk/release/*.apk
    
    publishing:
      email:
        recipients:
          - abuk10252@gmail.com
        notify:
          success: true
          failure: true
